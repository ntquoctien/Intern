# AcademicService Infrastructure

Infrastructure code for AcademicService will be added in a later backend phase.

This phase now includes an EF Core DbContext scaffold, an initial migration, and SQL Server connection wiring via user secrets or environment variables. Repositories, RabbitMQ, gRPC, Docker, and API Gateway integration are still out of scope.
